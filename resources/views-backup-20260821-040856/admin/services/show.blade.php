@extends('layouts.admin')
@section('title', $service->name.' | Services')
@section('heading', $service->name)
@section('content')<a href="{{ route('admin.services.index') }}" class="text-sm font-semibold text-brand-600">&larr; Services</a><div class="mt-6 grid gap-5 lg:grid-cols-[1fr_1.3fr]"><div class="rounded-xl border border-mist-200 bg-white p-6"><p class="text-sm text-slate-500">Category</p><p class="font-semibold">{{ $service->category->name ?? 'Uncategorized' }}</p><p class="mt-5 text-sm text-slate-500">Description</p><p class="mt-1 text-sm leading-relaxed">{{ $service->description ?: 'No description.' }}</p><p class="mt-5 text-sm text-slate-500">Price</p><p class="font-semibold">{{ $service->formatted_price }}</p></div><div class="rounded-xl border border-mist-200 bg-white p-6"><div class="flex items-center justify-between"><h2 class="font-display font-bold">Application fields</h2><a class="text-sm font-semibold text-brand-600" href="{{ route('admin.fields.index', $service->id) }}">Manage fields</a></div><ul class="mt-4 space-y-2 text-sm">@forelse($service->fields as $field)<li class="flex justify-between rounded-lg bg-mist-50 p-3"><span>{{ $field->label }}</span><span class="text-slate-500">{{ $field->field_type }}{{ $field->is_required ? ' · required' : '' }}</span></li>@empty<li class="text-slate-500">No fields configured.</li>@endforelse</ul></div></div>
@endsection
