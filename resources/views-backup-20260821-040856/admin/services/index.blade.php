@extends('layouts.admin')
@section('title', 'Services | Digital Star Consultants')
@section('heading', 'Services')
@section('content')<div class="flex flex-wrap items-center justify-between gap-3"><p class="text-sm text-slate-500">Manage public services and their application fields.</p><a href="#service-form" class="rounded-lg bg-brand-600 px-4 py-2.5 text-sm font-semibold text-white">New service</a></div><form id="service-form" method="POST" action="{{ route('admin.services.store') }}" class="mt-5 rounded-xl border border-mist-200 bg-white p-5"><h2 class="font-display font-bold">Create service</h2>@csrf<div class="mt-4 grid gap-4 md:grid-cols-3"><input class="rounded-lg border border-mist-200 p-3 text-sm" name="name" required placeholder="Service name"><select class="rounded-lg border border-mist-200 p-3 text-sm" name="service_category_id" required><option value="">Category</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select><input class="rounded-lg border border-mist-200 p-3 text-sm" type="number" name="price" step="0.01" placeholder="Price"></div><textarea class="mt-4 w-full rounded-lg border border-mist-200 p-3 text-sm" name="description" placeholder="Description"></textarea><button class="mt-4 rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Save service</button></form><div class="mt-6 grid gap-4 md:grid-cols-2">@forelse($services as $service)<a href="{{ route('admin.services.show', $service->id) }}" class="rounded-xl border border-mist-200 bg-white p-5 hover:border-brand-600"><div class="flex justify-between gap-3"><h2 class="font-display font-bold">{{ $service->name }}</h2><span class="text-xs {{ $service->is_active ? 'text-emerald-600' : 'text-slate-400' }}">{{ $service->is_active ? 'Active' : 'Inactive' }}</span></div><p class="mt-2 text-sm text-slate-500">{{ $service->category->name ?? 'Uncategorized' }}</p><p class="mt-4 text-sm">{{ $service->formatted_price }} <span class="text-slate-400">· {{ $service->fields_count ?? $service->fields->count() }} fields</span></p></a>@empty<p class="text-sm text-slate-500">No services found.</p>@endforelse</div>
@endsection
