@extends('layouts.admin')
@section('page_title','Application builder')
@section('content')<div class="builder-head"><div><span class="eyebrow">{{ $service->category->full_path ?? 'SERVICE' }}</span><h2>{{ $service->name }}</h2><p>Configure exactly what customers must provide for this service.</p></div><a class="button button-outline" href="{{ route('admin.services.index') }}">Back to services</a></div><div class="builder-grid"><section class="panel"><div class="panel-head"><h2>Customer fields</h2><span>{{ $fields->count() }} fields</span></div><div class="field-builder">@foreach($fields as $field)<div class="builder-field"><span class="drag">⋮⋮</span><div><b>{{ $field->label }}</b><small>{{ $field->type_label }} · {{ $field->is_required?'Required':'Optional' }}</small></div><code>{{ $field->field_key }}</code></div>@endforeach</div></section><section class="panel"><span class="eyebrow">ADD FIELD</span><h2>New question</h2><form id="field-form" class="stack-form">@csrf<label>Label<input name="label" required placeholder="e.g. NIDA Number"></label><label>Type<select name="field_type">@foreach($fieldTypes as $key=>$label)<option value="{{ $key }}">{{ $label }}</option>@endforeach</select></label><label>Options <small>One per line for select/radio</small><textarea name="options" placeholder="Individual&#10;Company"></textarea><label class="check"><input type="checkbox" name="is_required" value="1" checked> Required</label><button class="button button-yellow">Add field</button></form></section></div><script>document.getElementById('field-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const raw=fd.get('options')||'';fd.delete('options');raw.split('\n').map(x=>x.trim()).filter(Boolean).forEach(x=>fd.append('options[]',x));const r=await fetch('{{ route('admin.fields.store',$service) }}',{method:'POST',headers:{'Accept':'application/json','X-CSRF-TOKEN':document.querySelector('meta[name=csrf-token]').content},body:fd});if(r.ok)location.reload();else alert('Could not add field.');});</script>@endsection
