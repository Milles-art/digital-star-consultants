@extends('layouts.admin')
@section('title', $submission->reference_number.' | Submissions')
@section('heading', $submission->reference_number)
@section('content')<a href="{{ route('admin.submissions.index') }}" class="text-sm font-semibold text-brand-600">&larr; Submissions</a><div class="mt-6 grid gap-5 lg:grid-cols-[1fr_1fr]"><div class="rounded-xl border border-mist-200 bg-white p-6"><div class="flex justify-between"><h2 class="font-display font-bold">Request details</h2><span class="capitalize text-brand-600">{{ str_replace('_',' ',$submission->status) }}</span></div><dl class="mt-5 space-y-4 text-sm"><div><dt class="text-slate-500">Customer</dt><dd class="font-semibold">{{ $submission->customer_name }}</dd></div><div><dt class="text-slate-500">Phone</dt><dd>{{ $submission->customer_phone }}</dd></div><div><dt class="text-slate-500">Email</dt><dd>{{ $submission->customer_email ?: 'Not provided' }}</dd></div><div><dt class="text-slate-500">Service</dt><dd>{{ $submission->service->name ?? 'N/A' }}</dd></div><div><dt class="text-slate-500">Notes</dt><dd>{{ $submission->customer_notes ?: 'None' }}</dd></div></dl></div><div class="rounded-xl border border-mist-200 bg-white p-6"><h2 class="font-display font-bold">Submitted fields</h2><dl class="mt-5 space-y-4 text-sm">@forelse($submission->values as $value)<div><dt class="text-slate-500">{{ $value->field->label ?? 'Field' }}</dt><dd class="font-semibold">{{ $value->getValueForDisplay() }}</dd></div>@empty<p class="text-slate-500">No extra fields.</p>@endforelse</dl></div></div>
@endsection
