@extends('layouts.admin')
@section('title', 'Reports | Digital Star Consultants')
@section('heading', 'Reports')
@section('content')<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Requests',$total_submissions ?? 0],['Completed',$completed_submissions ?? 0],['Pending',$pending_submissions ?? 0],['Completion rate',($completion_rate ?? 0).'%']] as [$label,$value])<div class="rounded-xl border border-mist-200 bg-white p-5"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 font-display text-3xl font-extrabold text-brand-600">{{ $value }}</p></div>@endforeach</div><div class="mt-8 grid gap-4 md:grid-cols-3"><a href="{{ route('admin.reports.daily') }}" class="rounded-xl border border-mist-200 bg-white p-5 font-display font-bold hover:border-brand-600">Daily report <span class="block mt-2 text-sm font-normal text-slate-500">Today’s request activity.</span></a><a href="{{ route('admin.reports.weekly') }}" class="rounded-xl border border-mist-200 bg-white p-5 font-display font-bold hover:border-brand-600">Weekly report <span class="block mt-2 text-sm font-normal text-slate-500">Daily breakdown for a week.</span></a><a href="{{ route('admin.reports.staff-performance') }}" class="rounded-xl border border-mist-200 bg-white p-5 font-display font-bold hover:border-brand-600">Staff performance <span class="block mt-2 text-sm font-normal text-slate-500">Work completed by team member.</span></a></div>
@endsection
