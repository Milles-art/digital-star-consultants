@extends('layouts.app')
@section('title', 'Request '.$submission->reference_number.' | Digital Star Consultants')
@section('content')
<section class="mx-auto max-w-4xl px-4 py-14 sm:px-6 lg:px-8"><a href="{{ route('public.submissions.track.form') }}" class="text-sm font-semibold text-brand-600">&larr; Track another request</a><div class="mt-8 flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-bold uppercase tracking-[.2em] text-brand-600">{{ $submission->reference_number }}</p><h1 class="mt-3 font-display text-4xl font-extrabold">{{ $submission->service->name ?? 'Service request' }}</h1></div><span class="rounded-full bg-brand-50 px-4 py-2 text-sm font-bold capitalize text-brand-700">{{ str_replace('_', ' ', $submission->status) }}</span></div><div class="mt-10 grid gap-5 md:grid-cols-2"><div class="surface-card rounded-2xl p-6"><h2 class="font-display font-bold">Request details</h2><dl class="mt-4 space-y-3 text-sm"><div><dt class="text-slate-500">Submitted</dt><dd class="font-semibold">{{ $submission->created_at->format('d M Y, H:i') }}</dd></div>@if($submission->preferred_date)<div><dt class="text-slate-500">Preferred date</dt><dd class="font-semibold">{{ $submission->preferred_date->format('d M Y') }}</dd></div>@endif</dl></div><div class="surface-card rounded-2xl p-6"><h2 class="font-display font-bold">Progress</h2><p class="mt-4 text-sm leading-relaxed text-slate-600">Our team will contact you when additional information is needed or your request is ready.</p>@if($submission->staff_notes)<p class="mt-4 rounded-lg bg-mist-50 p-3 text-sm text-slate-600">{{ $submission->staff_notes }}</p>@endif</div></div></section>
@endsection
